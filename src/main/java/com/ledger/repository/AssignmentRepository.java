package com.ledger.repository; import com.ledger.model.Assignment; import org.springframework.data.jpa.repository.JpaRepository; import org.springframework.stereotype.Repository; import java.util.List; @Repository public interface AssignmentRepository extends JpaRepository<Assignment, Long> { List<Assignment> findByInstructorId(Long instructorId); List<Assignment> findByCourseId(Long courseId); }
